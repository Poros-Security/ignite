# NetOps Toolkit (Command Injection Lab)

Stack:

- Python Flask
- SQLite (history logging)

Pages:

- `/ping`
- `/nslookup`
- `/traceroute`
- `/history`

This app is intentionally vulnerable for command injection training. User input is passed directly into shell commands.
